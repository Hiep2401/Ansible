---
- name: Cau hinh tren router
  hosts: all
  gather_facts: no

  tasks:
    - name: Show ip
      ios_command:
        commands:
          - show ip int brief
          - show clock
      register: output

    - debug: var=output.stdout_lines
